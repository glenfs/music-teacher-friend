You are working in the Clefira Godot project.

TASK: Update **Sight Reading → Chords** so it is **always Grand Staff** (Treble + Bass together). We are removing the Treble/Bass toggle entirely.

SCOPE / SAFETY (NO SIDE EFFECTS)
- Only modify the Sight Reading → Chords menu + the chord-generation/gameplay code it depends on.
- Do NOT change other modes (Notes / Note Flow / Rhythm Flow / Note Chase / Ear Training).
- Keep existing UI theme/styles unless needed for the new grand-staff UI.
- Keep existing scoring/progress logic unless it directly depends on treble/bass toggle.

GOAL
We want piano-like voicings where chord tones can be split across two clefs (grand staff), including inversions/spread voicings commonly seen in piano scores.

REQUIREMENTS
1) Grand Staff always ON
- Remove/hide the UI controls for selecting Treble vs Bass.
- Internally, chord questions must be rendered and evaluated as a combined grand staff chord.

2) Chord voicing distribution across clefs
Generate chord “shapes” that place chord tones across both staves, like piano scores. Support these common distributions (examples, not exhaustive):
- 2 notes Treble + 2 notes Bass
- 3 Treble + 1 Bass
- 1 Treble + 3 Bass
- 4 Treble + 1 Bass
- 2 Treble + 3 Bass
- 3 Treble + 2 Bass
And other realistic piano-style combinations.
Notes:
- Chords may be triads or 7ths depending on current settings (Maj/Min, Aug/Dim, Sus, 7th, etc.) — preserve existing chord family options.
- Inversions must still work, but now the inversion can be split across staves (e.g., chord tones distributed between hands).

3) “Piano realistic” constraints (please implement)
- Avoid awkward/unplayable spreads:
  - Cap the overall span (lowest bass note to highest treble note) to a reasonable range (e.g., <= 2.5–3 octaves, configurable).
  - Keep bass staff notes generally below middle C and treble staff notes generally above (with some overlap allowed).
- Avoid excessive note density:
  - Limit max simultaneous notes (based on existing chord type) and ensure duplicates/stacked unisons are avoided unless deliberately needed.
- Voice-leading / spacing heuristics (simple is fine):
  - Bass staff: prefer wider spacing between low notes; avoid “muddy” close intervals below ~C3.
  - Treble staff: allow closer spacing but avoid extreme cluster unless chord quality requires.
- Enforce unique chord tones rules:
  - Triads: allow optional doubling (common in piano) but do not over-double to the point of ambiguity.
  - 7ths: do not omit the 7th unless a “simplify” flag is enabled (default false).

4) Answer evaluation
- User answers are still chord identification (quality + root + inversion behavior), not “which notes are on which clef”.
- The correctness should be based on the underlying chord identity/inversion rules you already use, not the exact voicing layout.
- Ensure the voicing generator never produces a chord that could be interpreted as a different chord than intended (avoid ambiguous stacks).

5) Rendering
- Display chord notes in both staves (grand staff). Use the existing notation rendering approach (OSMD, custom, sprites, etc. — whatever the project already uses).
- Notes must appear on correct staff positions.
- If you have a staff-clef boundary overlap zone, ensure notes go to the correct staff based on pitch.

6) UI changes on the Chords setup screen
- Remove Treble/Bass selection.
- Replace with a small label: “Grand Staff (Piano)” and optionally a toggle:
  - “Spread Voicings: On/Off” (default ON)
  - If OFF, keep all chord tones closer (still grand staff but more compact).
(If adding toggles is risky, skip; but keep the design ready.)

7) Defaults
- Keep current key signature restrictions (MVP may be only C). Do not widen scope unless already supported.
- Keep existing chord families and inversion toggle behavior.
- Preserve the “Let’s Play” flow.

DELIVERABLES
A) Identify the exact files/scenes/scripts involved (paths).
B) Implement grand-staff-only chord mode and remove treble/bass toggle.
C) Implement a voicing generator:
   - Inputs: chord (root + quality + inversion), desired note count, allowed pitch ranges for bass/treble, spread profile
   - Output: a list of pitches assigned to staff = {bass|treble}
D) Add automated sanity checks (lightweight):
   - Generate 500 random chords; assert:
     - All notes are within allowed ranges
     - Staff assignment matches pitch
     - No voicing violates span constraints
     - Underlying chord identity matches intended target

IMPLEMENTATION NOTES
- Keep changes minimal, avoid refactors across unrelated systems.
- Prefer adding a new module/class like `ChordVoicingGenerator.gd` (or equivalent) and using it only from the Chords mode.
- If there is an existing “range selector” for treble/bass, replace it with two fixed ranges:
  - Bass range default: C2–E4
  - Treble range default: A3–C6
  - Overlap A3–E4 is allowed but assign by pitch threshold.
- Provide comments in code for the heuristics so we can tune later.

TESTING STEPS (manual)
1) Open Sight Reading → Chords.
2) Confirm no Treble/Bass toggle exists.
3) Start 20 rounds:
   - Verify chord is drawn across grand staff.
   - Confirm chord types match the label/expected logic.
   - Ensure spread looks piano-like (not random nonsense).
4) Ensure other screens/modes are unchanged.

Finally: summarize what you changed and where, and list any tuning constants you introduced (ranges, max span, etc).