extends RefCounted

# Background & surfaces
const BG_PRIMARY := Color(0.06, 0.09, 0.16, 1.0)
const BG_SIDEBAR := Color(0.08, 0.12, 0.20, 1.0)
const BG_CARD := Color(0.10, 0.15, 0.24, 0.90)
const BG_INPUT := Color(0.08, 0.12, 0.20, 0.80)
const BG_TAB_ACTIVE := Color(0.14, 0.20, 0.30, 1.0)
const BG_TAB_INACTIVE := Color(0.08, 0.12, 0.20, 0.60)

# Accents
const ACCENT_GOLD := Color(0.9098, 0.6275, 0.1255, 1.0)
const ACCENT_TEAL := Color(0.475, 0.82, 0.80, 1.0)
const ACCENT_GREEN := Color(0.30, 0.78, 0.40, 1.0)
const ACCENT_RED := Color(0.90, 0.32, 0.30, 1.0)
const ACCENT_BLUE := Color(0.30, 0.55, 0.85, 1.0)
const ACCENT_PURPLE := Color(0.55, 0.40, 0.80, 1.0)

# Text
const TEXT_PRIMARY := Color(0.92, 0.96, 1.0, 1.0)
const TEXT_SECONDARY := Color(0.72, 0.78, 0.86, 0.80)
const TEXT_MUTED := Color(0.55, 0.60, 0.68, 0.70)
const TEXT_DARK := Color(0.08, 0.08, 0.10, 1.0)

# Borders
const BORDER_SUBTLE := Color(0.30, 0.36, 0.46, 0.40)
const BORDER_ACTIVE := Color(0.475, 0.82, 0.80, 0.60)

# Status badge colors
const STATUS_ASSIGNED := Color(0.55, 0.65, 0.80, 0.90)
const STATUS_WORKING := Color(0.90, 0.70, 0.20, 0.90)
const STATUS_POLISHING := Color(0.30, 0.78, 0.40, 0.90)
const STATUS_PERFORMED := Color(0.475, 0.82, 0.80, 0.90)
const STATUS_PAUSED := Color(0.55, 0.55, 0.55, 0.70)

# Layout
const SIDEBAR_WIDTH := 240.0
const CARD_RADIUS := 12
const CARD_PADDING := 14
const SECTION_GAP := 16
const INPUT_HEIGHT := 36
const BTN_HEIGHT := 34
const FONT_SIZE_TITLE := 22
const FONT_SIZE_HEADING := 17
const FONT_SIZE_BODY := 14
const FONT_SIZE_SMALL := 12
const FONT_SIZE_TAB := 15

# Star rating
const STAR_FILLED := Color(1.0, 0.85, 0.20, 1.0)
const STAR_EMPTY := Color(0.40, 0.42, 0.44, 0.40)
