# Performance Implementation Prompt - MusicEd Godot Project

## Task
Implement all remaining menu performance optimizations in the MusicEd Godot project. Focus on the home screen, settings screens, and mode selection menus (NOT in-game gameplay).
Make sure you dont change the current menu screen styles setting. and the recent performance improvemnts we made. check if any of the beloiw make sense to implement and implement only those. 

## Context
- **Engine:** Godot 4.6
- **Main script:** `scripts/interval_birds.gd` (~22k lines)
- **Target:** Mobile (Android) + Desktop
- **Previous work:** Some caching exists (`_sight_notes_section_style_cached`) but critical optimizations not completed

## Critical Issues to Fix

### 1. STYLEBOX CACHING (CRITICAL - Do First)

**Problem:** 92 instances of `StyleBoxFlat.new()` and `.duplicate()` create new objects every button style call.

**Location:** `scripts/interval_birds.gd` - Lines 5664-5850

**Functions to refactor:**
- `_style_button()` - Line 5664
- `_style_settings_button()` - Line 5702
- `_style_interval_option_toggle()` - Line 5741
- `_style_menu_toggle()` - Line 5793
- `_style_include_minor_toggle()` - Line 5763
- `_style_descending_intervals_toggle()` - Line 5767
- `_style_harmonic_intervals_toggle()` - Line 5771

**Current pattern (BAD):**
```gdscript
func _style_button(btn: Button) -> void:
    var pal := _theme_button_palette()
    var normal := StyleBoxFlat.new()  # Creates new every call!
    normal.bg_color = pal["base_bg"]
    # ... 20 more property sets ...
    btn.add_theme_stylebox_override("normal", normal)
    
    var hover := normal.duplicate()  # Duplicates every call!
    hover.bg_color = pal["base_hover"]
    btn.add_theme_stylebox_override("hover", hover)
```

**Required pattern (GOOD):**
```gdscript
# Add to variable declarations (around line 460):
var _button_style_cache: Dictionary = {
    "normal": null,
    "hover": null,
    "pressed": null,
    "settings_normal": null,
    "settings_hover": null,
    "settings_pressed": null,
    "toggle_on_normal": null,
    "toggle_on_hover": null,
    "toggle_on_pressed": null,
    "toggle_off_normal": null,
    "toggle_off_hover": null,
    "toggle_off_pressed": null
}
var _button_style_cache_theme_id: String = ""

# Add new function after _ready() (around line 1050):
func _cache_button_styles() -> void:
    var pal := _theme_button_palette()
    var contract := _menu_style_contract()
    
    # Clear old cache
    for key in _button_style_cache:
        if _button_style_cache[key] != null:
            _button_style_cache[key].free()
    
    # Base button styles
    _button_style_cache["normal"] = _create_button_stylebox(
        pal["base_bg"], pal["base_border"], pal["base_hover"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    _button_style_cache["hover"] = _create_button_stylebox(
        pal["base_hover"], pal["base_border"], pal["base_pressed"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    _button_style_cache["pressed"] = _create_button_stylebox(
        pal["base_pressed"], pal["base_border"], pal["base_bg"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    
    # Settings button styles
    _button_style_cache["settings_normal"] = _create_button_stylebox(
        pal["settings_bg"], pal["settings_border"], pal["settings_hover"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    _button_style_cache["settings_hover"] = _create_button_stylebox(
        pal["settings_hover"], pal["settings_border"], pal["settings_pressed"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    _button_style_cache["settings_pressed"] = _create_button_stylebox(
        pal["settings_pressed"], pal["settings_border"], pal["settings_bg"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    
    # Toggle styles (on state)
    _button_style_cache["toggle_on_normal"] = _create_toggle_stylebox(
        pal["toggle_on_bg"], pal["toggle_on_border"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    _button_style_cache["toggle_on_hover"] = _create_toggle_stylebox(
        pal["toggle_on_bg"].lightened(0.08), pal["toggle_on_border"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    _button_style_cache["toggle_on_pressed"] = _create_toggle_stylebox(
        pal["toggle_on_bg"].darkened(0.08), pal["toggle_on_border"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    
    # Toggle styles (off state)
    _button_style_cache["toggle_off_normal"] = _create_toggle_stylebox(
        pal["toggle_off_bg"], pal["toggle_off_border"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    _button_style_cache["toggle_off_hover"] = _create_toggle_stylebox(
        pal["toggle_off_bg"].lightened(0.08), pal["toggle_off_border"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    _button_style_cache["toggle_off_pressed"] = _create_toggle_stylebox(
        pal["toggle_off_bg"].darkened(0.08), pal["toggle_off_border"],
        contract.get("shadow_button_alpha", 0.28),
        contract.get("shadow_button_size", 4)
    )
    
    _button_style_cache_theme_id = _home_tokens.theme_id()

func _create_button_stylebox(bg: Color, border: Color, hover_bg: Color, shadow_alpha: float, shadow_size: int) -> StyleBoxFlat:
    var sb := StyleBoxFlat.new()
    sb.bg_color = bg
    sb.corner_radius_top_left = 12
    sb.corner_radius_top_right = 12
    sb.corner_radius_bottom_left = 12
    sb.corner_radius_bottom_right = 12
    sb.border_color = border
    sb.border_width_left = 2
    sb.border_width_top = 2
    sb.border_width_right = 2
    sb.border_width_bottom = 2
    sb.shadow_color = Color(0.0, 0.0, 0.0, shadow_alpha)
    sb.shadow_size = shadow_size
    return sb

func _create_toggle_stylebox(bg: Color, border: Color, shadow_alpha: float, shadow_size: int) -> StyleBoxFlat:
    var sb := StyleBoxFlat.new()
    sb.bg_color = bg
    sb.corner_radius_top_left = 16
    sb.corner_radius_top_right = 16
    sb.corner_radius_bottom_left = 16
    sb.corner_radius_bottom_right = 16
    sb.border_color = border
    sb.border_width_left = 3
    sb.border_width_top = 3
    sb.border_width_right = 3
    sb.border_width_bottom = 3
    sb.shadow_color = Color(0.0, 0.0, 0.0, shadow_alpha)
    sb.shadow_size = shadow_size
    return sb

# Modify _ready() to call cache function (add after line 1034):
_load_menu_setup_style_resources()
_cache_button_styles()  # ADD THIS LINE
_build_ui()

# Modify _invalidate_button_palette_cache() to also invalidate style cache (line 5569):
func _invalidate_button_palette_cache() -> void:
    _cached_button_palette = {}
    _cached_palette_theme_id = ""
    _button_style_cache_theme_id = ""  # Force style cache rebuild

# Now refactor _style_button() to use cache:
func _style_button(btn: Button) -> void:
    if _button_style_cache_theme_id != _home_tokens.theme_id():
        _cache_button_styles()
    
    btn.add_theme_stylebox_override("normal", _button_style_cache["normal"])
    btn.add_theme_stylebox_override("hover", _button_style_cache["hover"])
    btn.add_theme_stylebox_override("pressed", _button_style_cache["pressed"])
    
    var pal := _theme_button_palette()
    var base_text: Color = _pick_readable_text_color(pal["base_bg"])
    btn.add_theme_color_override("font_color", base_text)
    btn.add_theme_color_override("font_hover_color", _pick_readable_text_color(pal["base_hover"]))
    btn.add_theme_color_override("font_pressed_color", _pick_readable_text_color(pal["base_pressed"]))
    btn.add_theme_color_override("font_outline_color", Color(0.0, 0.0, 0.0, 0.35))
    btn.add_theme_constant_override("outline_size", 1)
    
    if not btn.has_meta("_hover_feedback_connected"):
        btn.set_meta("_hover_feedback_connected", true)
        _connect_button_hover_feedback(btn)
```

**Similar refactoring required for:**
- `_style_settings_button()` - Use `settings_*` cached styles
- `_style_interval_option_toggle()` - Use `toggle_*` cached styles
- `_style_menu_toggle()` - Use `toggle_*` cached styles

---

### 2. DISABLE SCROLL FOLLOW_FOCUS (HIGH PRIORITY)

**Problem:** `_home_scroll.follow_focus = true` causes scroll on every focus change (50+ buttons = 50+ scroll operations)

**Location:** Line 2357

**Change:**
```gdscript
# FROM:
_home_scroll.follow_focus = true

# TO:
_home_scroll.follow_focus = false  # Manual scroll control prevents layout thrashing
```

---

### 3. PRE-COMPUTE THEME PALETTES (HIGH PRIORITY)

**Problem:** Theme palette dictionary (200+ entries) rebuilt on every theme change

**Location:** Lines 5573-5653 `_theme_button_palette()`

**Add new variable (around line 997):**
```gdscript
var _theme_palettes_precomputed: Dictionary = {}
```

**Add new function after _cache_button_styles():**
```gdscript
func _precompute_theme_palettes() -> void:
    var original_theme := _home_tokens.theme_id()
    
    for theme_id in ["golden_harvest", "azure_cascade", "slate_foundry", "crimson_nocturne", "rose_velvet"]:
        _home_tokens.set_theme(theme_id)
        _theme_palettes_precomputed[theme_id] = _compute_theme_palette_internal()
    
    _home_tokens.set_theme(original_theme)

func _compute_theme_palette_internal() -> Dictionary:
    # Extract the internal palette computation logic from _theme_button_palette()
    # This should be the 200+ line dictionary creation without caching logic
    var result: Dictionary = {}
    var contract := _menu_style_contract()
    # ... copy the existing dictionary creation logic from _theme_button_palette() ...
    return result

# Modify _theme_button_palette() to use precomputed:
func _theme_button_palette() -> Dictionary:
    var id := _home_tokens.theme_id()
    if _theme_palettes_precomputed.has(id):
        return _theme_palettes_precomputed[id]
    # Fallback (should never happen if _precompute_theme_palettes() called in _ready())
    return _compute_theme_palette_internal()

# Add call in _ready() after _cache_button_styles():
_precompute_theme_palettes()  # ADD THIS LINE
```

---

### 4. REPLACE HOVER TWEENS WITH ANIMATIONPLAYER (MEDIUM PRIORITY)

**Problem:** `_connect_card_hover_feedback()` creates new tween on every mouse enter/exit

**Location:** Lines 5842-5913

**Step 1: Add AnimationPlayer to scene**
- Open `scenes/IntervalBirds.tscn` in Godot editor
- Add `AnimationPlayer` node as child of `IntervalBirds`
- Name it `HoverAnimationPlayer`
- Create two animations:
  - `card_scale_up`: scale from 1.0 to 1.025 over 0.12s (TRANS_QUAD, EASE_OUT)
  - `card_scale_down`: scale from 1.025 to 1.0 over 0.15s (TRANS_QUAD, EASE_OUT)

**Step 2: Update code**
```gdscript
# Add variable (around line 460):
var _hover_anim: AnimationPlayer

# Initialize in _ready() (after line 1034):
_hover_anim = get_node_or_null("HoverAnimationPlayer")

# Replace _connect_card_hover_feedback():
func _connect_card_hover_feedback(card: Control, btn: Button) -> void:
    if card == null or btn == null or _hover_anim == null:
        return
    
    card.pivot_offset = card.size * 0.5
    card.pivot_offset = card.size * 0.5
    card.mouse_filter = Control.MOUSE_FILTER_PASS
    
    var hover_enter := func() -> void:
        if card == null or not is_instance_valid(card):
            return
        card.pivot_offset = card.size * 0.5
        _hover_anim.play("card_scale_up")
        
        var sb: Variant = card.get_theme_stylebox("panel") if card is PanelContainer else null
        if sb is StyleBoxFlat:
            var hover_sb := sb.duplicate() as StyleBoxFlat
            hover_sb.shadow_size = mini(hover_sb.shadow_size + 4, 16)
            hover_sb.shadow_color = Color(hover_sb.shadow_color.r, hover_sb.shadow_color.g, hover_sb.shadow_color.b, minf(hover_sb.shadow_color.a + 0.06, 0.5))
            card.add_theme_stylebox_override("panel", hover_sb)
            card.set_meta("_pre_hover_sb", sb)
    
    var hover_exit := func() -> void:
        if card == null or not is_instance_valid(card):
            return
        card.pivot_offset = card.size * 0.5
        _hover_anim.play("card_scale_down")
        
        if card.has_meta("_pre_hover_sb"):
            var old_sb: Variant = card.get_meta("_pre_hover_sb")
            if old_sb is StyleBoxFlat:
                card.add_theme_stylebox_override("panel", old_sb as StyleBoxFlat)
            card.remove_meta("_pre_hover_sb")
    
    btn.mouse_entered.connect(hover_enter)
    btn.mouse_exited.connect(hover_exit)
```

---

### 5. CONDITIONAL VISIBILITY UPDATES (LOW PRIORITY)

**Problem:** `.visible` set without checking current state

**Location:** Multiple `_refresh_*` and `_show_*` functions

**Pattern to apply:**
```gdscript
# FROM:
if _tutorial_panel != null:
    _tutorial_panel.visible = false

# TO:
if _tutorial_panel != null and _tutorial_panel.visible:
    _tutorial_panel.visible = false
```

**Apply to these functions:**
- `_show_home()` - Lines 12844-12950 (10+ visibility sets)
- `_show_game()` - Lines 12953-13050 (5+ visibility sets)
- `_refresh_background_scene_emphasis()` - Lines 4712-4730

---

### 6. BATCH LAYOUT CHANGES (LOW PRIORITY)

**Problem:** Multiple theme constant changes trigger multiple relayouts

**Location:** `_finalize_home_menu_layout()` and similar functions

**Pattern to apply:**
```gdscript
# FROM:
_home_panel.add_theme_constant_override("separation", 12)
_home_q_row.add_theme_constant_override("separation", 10)
_home_mode_buttons_row.add_theme_constant_override("separation", 10)

# TO:
_home_panel.set_process(false)  # Pause processing
_home_panel.add_theme_constant_override("separation", 12)
_home_q_row.add_theme_constant_override("separation", 10)
_home_mode_buttons_row.add_theme_constant_override("separation", 10)
_home_panel.set_process(true)  # Resume (single relayout)
```

---

## Implementation Checklist

### CRITICAL (Do First)
- [ ] Add `_button_style_cache` dictionary variable
- [ ] Implement `_cache_button_styles()` function
- [ ] Implement `_create_button_stylebox()` helper
- [ ] Implement `_create_toggle_stylebox()` helper
- [ ] Refactor `_style_button()` to use cache
- [ ] Refactor `_style_settings_button()` to use cache
- [ ] Refactor `_style_interval_option_toggle()` to use cache
- [ ] Refactor `_style_menu_toggle()` to use cache
- [ ] Call `_cache_button_styles()` in `_ready()`
- [ ] Update `_invalidate_button_palette_cache()` to clear style cache

### HIGH (Do Second)
- [ ] Change `_home_scroll.follow_focus = false` (line 2357)
- [ ] Add `_theme_palettes_precomputed` variable
- [ ] Implement `_precompute_theme_palettes()` function
- [ ] Modify `_theme_button_palette()` to use precomputed
- [ ] Call `_precompute_theme_palettes()` in `_ready()`

### MEDIUM (Do Third)
- [ ] Add `AnimationPlayer` node to `IntervalBirds.tscn`
- [ ] Create `card_scale_up` animation
- [ ] Create `card_scale_down` animation
- [ ] Add `_hover_anim` variable
- [ ] Initialize `_hover_anim` in `_ready()`
- [ ] Refactor `_connect_card_hover_feedback()` to use AnimationPlayer

### LOW (Do Last)
- [ ] Apply conditional visibility pattern to `_show_home()`
- [ ] Apply conditional visibility pattern to `_show_game()`
- [ ] Apply conditional visibility pattern to `_refresh_background_scene_emphasis()`
- [ ] Apply batch layout pattern to `_finalize_home_menu_layout()`

---

## Testing After Implementation

### 1. Verify No Errors
```
Run project in Godot editor
Watch for "Attempt to call on null instance" errors
Check Output panel for warnings
```

### 2. Performance Test
```
Press F5 in debug build to show profiler
Navigate: Home → Ear Settings → Mode Select → Start → Back → Home
Note FPS during navigation (should be 55-60, not 45-55)
Watch for GC spikes in profiler
```

### 3. Memory Test
```
Run with --verbose flag
Monitor "Object" count in output
Navigate menu for 2 minutes
Object count should be stable (not growing)
```

### 4. Visual Regression Test
```
Check all buttons render correctly
Verify hover effects still work
Confirm theme switching works
Test all toggle buttons
```

---

## Expected Results

| Metric | Before | After |
|--------|--------|-------|
| StyleBox allocations/menu switch | 150+ | 0-5 |
| Menu startup time | 2-3 sec | 1-1.5 sec |
| Navigation FPS | 45-55 | 55-60 |
| Memory (menu) | ~80 MB | ~50 MB |
| GC spikes | Every 2-3 sec | Rare (< 1/min) |
| Script line count | 22,116 | ~22,300 (slight increase OK) |

---

## Files to Modify

1. **scripts/interval_birds.gd** - Main refactoring target
2. **scenes/IntervalBirds.tscn** - Add AnimationPlayer node

## Files to NOT Modify

- `scripts/ear_training/*.gd` - Already refactored, leave alone
- `ui/components/*.gd` - Component scripts, leave alone
- `scripts/rhythm/*.gd` - Gameplay code, not menu
- `scripts/audio/*.gd` - Audio system, not menu

---

## Important Notes

1. **Do NOT break existing functionality** - All buttons must still work
2. **Test theme switching** - Ensure all 5 themes still apply correctly
3. **Preserve visual quality** - Cached styles must match original appearance
4. **Handle null cases** - Check for null before accessing cached resources
5. **Free old cache** - Call `.free()` on old StyleBox before replacing to prevent memory leaks

---

## If You Encounter Issues

### Issue: "StyleBox is already in use" error
**Solution:** Call `.duplicate()` when applying cached style to new control

### Issue: Button colors wrong after theme switch
**Solution:** Ensure `_cache_button_styles()` is called when theme changes

### Issue: AnimationPlayer not found
**Solution:** Verify node was added to scene tree, check node path

### Issue: Memory leak warnings
**Solution:** Ensure old cached StyleBox objects are freed before replacement

---

## Success Criteria

✅ All checklist items completed  
✅ No console errors in Godot Output  
✅ FPS ≥ 55 during menu navigation  
✅ Memory stable after 2 minutes in menu  
✅ All 5 themes render correctly  
✅ All buttons respond to hover/click  
✅ No visual regression from before changes
